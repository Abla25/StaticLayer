# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Local wrangler / Miniflare state (never commit)
.wrangler/
wrangler/state/

# Local dev / environment secrets — NEVER commit
.dev.vars*
!.dev.vars.example
.env
.env.*
!.env.example

# Logs / OS
*.log
npm-debug.log*
.DS_Store

# Editors / IDE
.idea/
.vscode/*
!.vscode/extensions.json

# Test artifacts
coverage/

# Owner's moderation key (generated at deploy, chmod 600) — NEVER commit
.admin-secret.txt

# installer-worker generated files (embedded assets + pre-bundled runtime)
apps/installer-worker/src/static-assets.ts
apps/installer-worker/src/runtime-bundle.ts
